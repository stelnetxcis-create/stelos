[package]
name = "touch_gestures"
version = "0.1.0"
edition = "2021"

[dependencies]
evdev = "0.13"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
opt-level = "z"
lto = true
strip = true
